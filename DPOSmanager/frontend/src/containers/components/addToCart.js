import React, { Component } from "react";

export default class AddToCart extends Component {
  render() {
    /* On click 
      -> set global state of focusedProduct to current product with only necessary attributes (id, name, quantity, price)
      -> redirect to quantity modal and retrieve focusedProduct 
      -> append to CurrentTransaction array with details (id, name, quantity, price)
      -> exit modal

      -> 
      
      */
    return <button></button>;
  }
}
